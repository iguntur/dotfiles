#!/bin/bash

## load bash profile
[ -n "$BASH_VERSION" ] && source ~/.bash_profile


# Load our dotfiles
#   Use it to configure your PATH, thus it being first in line.
source ~/.bootstrap


##
## more options here...
##
